<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Tabs -->
    <io.karim.MaterialTabs
       android:id="@+id/rankings_tabs"
       android:layout_width="match_parent"
       android:layout_height="@dimen/tab_height"
       android:background="@color/colourPrimary"
       app:mtIndicatorColor="@color/colourAccent"
       android:textColor="@color/colourInactiveTab"
       app:mtTextColorSelected="@android:color/white"
       app:mtSameWeightTabs="true"
       app:mtPaddingMiddle="false" />

    <!-- View Pager -->
    <android.support.v4.view.ViewPager
        android:id="@+id/rankings_view_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>